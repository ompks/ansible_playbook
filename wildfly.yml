---
    - hosts: 192.168.1.215
    
      become: yes
      become_method: sudo
      #remote_user: use
      #gather_facts: no
      #environment: 
      #  JAVA_HOME: /home/user1/weblogic_setup_file/jdk1.8.0_66
      tasks:
      - name: Run wildfly
        #shell:  nohup sh /home/oracle/wildfly-19.1.0.Final/bin/standalone.sh &
        command: bash -lc "cd /home/oracle/wildfly-19.1.0.Final/bin && nohup ./standalone.sh &"
